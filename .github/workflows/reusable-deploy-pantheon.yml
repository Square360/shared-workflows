name: Reusable Deploy to Pantheon DEV

on:
  workflow_call:
    inputs:
      pantheon_site:
        description: 'Pantheon site name'
        required: true
        type: string
      target_env:
        description: 'Target environment (default: dev)'
        required: false
        type: string
        default: 'dev'
      backup_hours_threshold:
        description: 'Hours threshold for backup check (default: 6)'
        required: false
        type: number
        default: 6
      backup_wait_minutes:
        description: 'Max minutes to wait for the LIVE backup to appear in backup:list before failing (default: 15). terminus backup:create gives up before a large files archive finishes; the verify step trusts the listing, not the exit code (S360-1076).'
        required: false
        type: number
        default: 15
      run_semantic_release:
        description: 'Whether to run semantic release first'
        required: false
        type: boolean
        default: true
      slack_channel:
        description: 'Slack channel for notifications (e.g., #deployments)'
        required: false
        type: string
      workflow_skip_terminus:
        description: 'Skip post-deployment Terminus commands (database updates, config import, cache rebuild)'
        required: false
        type: boolean
        default: false
    secrets:
      PANTHEON_SSH_KEY:
        required: true
      PANTHEON_MACHINE_TOKEN:
        required: true
      CI_GH_TOKEN:
        required: false
      SLACK_BOT_TOKEN:
        required: false

defaults:
  run:
    shell: bash

jobs:
  semantic_release:
    name: Semantic Release
    if: ${{ inputs.run_semantic_release }}
    uses: ./.github/workflows/reusable-semantic-release.yml
    secrets:
      CI_GH_TOKEN: ${{ secrets.CI_GH_TOKEN }}

  deploy_to_pantheon:
    name: Deploy To ${{ inputs.target_env }}
    needs: semantic_release
    if: ${{ always() && (needs.semantic_release.result == 'success' || needs.semantic_release.result == 'skipped') }}
    permissions:
      deployments: write
      contents: write
      pull-requests: read
    runs-on: ubuntu-latest
    outputs:
      env_url: ${{ steps.env_url.outputs.url }}
      error_message: ${{ steps.capture_error.outputs.error_message }}
      failed_step: ${{ steps.capture_error.outputs.failed_step }}
      backup_seconds: ${{ steps.verify_backup.outputs.backup_seconds }}
    steps:
    - uses: actions/checkout@v6
      with:
        # Ensure we checkout the target branch (master/main) for deployment
        ref: ${{ github.event.pull_request.base.ref || github.ref }}
        # Use shallow clone to let pantheon action handle unshallowing
        fetch-depth: 1

    - name: Setup PHP
      uses: shivammathur/setup-php@v2
      with:
        php-version: '8.4'
        tools: composer:v2

    - name: Add Bitbucket to known hosts
      run: |
        mkdir -p ~/.ssh
        ssh-keyscan bitbucket.org >> ~/.ssh/known_hosts

    - name: Get Composer cache directory
      id: composer-cache
      run: echo "dir=$(composer config cache-files-dir)" >> $GITHUB_OUTPUT

    - name: Cache Composer dependencies
      uses: actions/cache@v5
      with:
        path: ${{ steps.composer-cache.outputs.dir }}
        key: ${{ runner.os }}-composer-${{ hashFiles('**/composer.lock') }}
        restore-keys: ${{ runner.os }}-composer-

    - name: Install Composer dependencies
      run: composer install --no-dev --optimize-autoloader --no-interaction

    - name: deploy to Pantheon
      id: deploy_pantheon
      uses: pantheon-systems/push-to-pantheon@0.9.0
      env:
        COMPOSER_NO_SECURITY_BLOCKING: 1
      with:
        ssh_key: ${{ secrets.PANTHEON_SSH_KEY }}
        machine_token: ${{ secrets.PANTHEON_MACHINE_TOKEN }}
        site: ${{ inputs.pantheon_site }}
        target_env: ${{ inputs.target_env }}
        git_user_name: ${{ github.event.pull_request.user.login || github.actor }}
        git_user_email: ${{ github.event.pull_request.user.email || format('{0}@users.noreply.github.com', github.actor) }}
        git_commit_message: "Automated deployment from GitHub Actions"
        delete_old_environments: false
        clone_content: false

    - name: Check for last backup
      id: last_backup
      env:
        TERMINUS_MACHINE_TOKEN: ${{ secrets.PANTHEON_MACHINE_TOKEN }}
      run: |
        # Try to get backup info - if any error occurs, skip backup creation
        if last_timestamp=$(terminus backup:info --element database --format list --field date ${{ inputs.pantheon_site }}.live 2>&1); then
          echo "Successfully retrieved backup information from live environment"

          # Get the timestamp from the list element
          last_epoch="${last_timestamp%%.*}"

          # Get current epoch time
          current_epoch=$(date +%s)

          # Calculate difference in seconds
          time_diff=$((current_epoch - last_epoch))

          # Convert hours to seconds
          hours_calc=$((${{ inputs.backup_hours_threshold }} * 3600))

          # Check if difference is more than threshold
          if [ $time_diff -gt $hours_calc ]; then
            echo "The last backup was: $((time_diff / 3600)) hours ago, we need a new one."
            echo "needs_backup=true" >> $GITHUB_OUTPUT
          else
            echo "needs_backup=false" >> $GITHUB_OUTPUT
          fi
        else
          echo "Could not retrieve backup information from live environment (environment may not exist or no backups found). Skipping backup creation."
          echo "needs_backup=false" >> $GITHUB_OUTPUT
        fi

    # terminus backup:create waits on the Pantheon workflow but gives up before a
    # large files archive finishes (TEDCO, 3.6 GB: terminus exited 1 at 2m34s,
    # Pantheon finished at 3m43s). Its exit code is therefore not the outcome.
    # Create without failing the job, then verify by listing (S360-1076).
    - name: Create backup on live
      id: create_backup
      if: ${{ steps.last_backup.outputs.needs_backup == 'true' }}
      env:
        TERMINUS_MACHINE_TOKEN: ${{ secrets.PANTHEON_MACHINE_TOKEN }}
      run: |
        echo "Creating backup on live"
        echo "start_epoch=$(date +%s)" >> $GITHUB_OUTPUT
        terminus -n env:clear-cache ${{ inputs.pantheon_site }}.live
        if terminus -n backup:create "${{ inputs.pantheon_site }}.live" --keep-for=30 --no-interaction --quiet --yes; then
          echo "terminus reported the backup complete"
        else
          echo "::notice::terminus backup:create exited non-zero (rc=$?) — this usually means it stopped waiting before Pantheon finished. Verifying by listing."
        fi

    - name: Verify backup on live
      id: verify_backup
      if: ${{ steps.last_backup.outputs.needs_backup == 'true' }}
      env:
        TERMINUS_MACHINE_TOKEN: ${{ secrets.PANTHEON_MACHINE_TOKEN }}
        SITE: ${{ inputs.pantheon_site }}
        START: ${{ steps.create_backup.outputs.start_epoch }}
        WAIT_MINUTES: ${{ inputs.backup_wait_minutes }}
      run: |
        # A backup is real when BOTH the database and files elements list a
        # backup dated at or after this run's start. Poll every 20s up to
        # backup_wait_minutes; fail only if it never appears.
        deadline=$(( START + WAIT_MINUTES * 60 ))
        while :; do
          db=$(terminus backup:info "${SITE}.live" --element=database --format=list --field=date 2>/dev/null || echo 0)
          files=$(terminus backup:info "${SITE}.live" --element=files --format=list --field=date 2>/dev/null || echo 0)
          db=${db%%.*}; files=${files%%.*}
          now=$(date +%s)
          if [[ "${db:-0}" -ge "$START" && "${files:-0}" -ge "$START" ]]; then
            took=$(( now - START ))
            printf 'backup verified: database %s, files %s — took %dm%02ds\n' "$(date -u -d @"$db" +%H:%M:%S)" "$(date -u -d @"$files" +%H:%M:%S)" $((took/60)) $((took%60))
            echo "backup_seconds=${took}" >> $GITHUB_OUTPUT
            break
          fi
          if [[ "$now" -ge "$deadline" ]]; then
            echo "::error::No LIVE backup newer than the step start appeared within ${WAIT_MINUTES} minutes (database=${db:-none}, files=${files:-none}, start=${START})."
            exit 1
          fi
          echo "waiting for backup… (db=${db:-none} files=${files:-none}, $(( (deadline - now) / 60 ))m left)"
          sleep 20
        done

    - name: Run config imports and db updates
      if: ${{ !inputs.workflow_skip_terminus }}
      id: drush_commands
      env:
        TERMINUS_MACHINE_TOKEN: ${{ secrets.PANTHEON_MACHINE_TOKEN }}
      run: |
        # Check PR title for order preference and verbosity
        # Use HERE document to safely handle all special characters
        read -r -d '' PR_TITLE <<'EOF' || true
        ${{ github.event.pull_request.title }}
        EOF

        # Set -v flag only when [verbose] tag is present in PR title
        UPDB_FLAGS="-y"
        if [[ "$PR_TITLE" == *"[verbose]"* ]]; then
          echo "🔍 Verbose updb enabled (detected [verbose] tag)"
          UPDB_FLAGS="-y -v"
        fi

        if [[ "$PR_TITLE" == *"[config-first]"* ]]; then
          echo "🔧 Running config import first (detected [config-first] tag)"
          terminus -n drush ${{ inputs.pantheon_site }}.${{ inputs.target_env }} -- cim -y
          terminus -n drush ${{ inputs.pantheon_site }}.${{ inputs.target_env }} -- updb $UPDB_FLAGS
        else
          echo "🔧 Running database update first (default order)"
          terminus -n drush ${{ inputs.pantheon_site }}.${{ inputs.target_env }} -- updb $UPDB_FLAGS
          terminus -n drush ${{ inputs.pantheon_site }}.${{ inputs.target_env }} -- cim -y
        fi

        # Always run cache rebuild last
        terminus -n env:clear-cache ${{ inputs.pantheon_site }}.${{ inputs.target_env }}

    - name: Get environment URL
      id: env_url
      env:
        TERMINUS_MACHINE_TOKEN: ${{ secrets.PANTHEON_MACHINE_TOKEN }}
      run: |
        URL=$(terminus env:view ${{ inputs.pantheon_site }}.${{ inputs.target_env }} --print)
        echo "url=$URL" >> $GITHUB_OUTPUT
        echo "Environment available at: $URL"

    - name: Capture error information
      id: capture_error
      if: ${{ failure() }}
      run: |
        # Determine which step failed and capture relevant error info
        if [[ "${{ steps.deploy_pantheon.conclusion }}" == "failure" ]]; then
          echo "failed_step=Pantheon Deployment" >> $GITHUB_OUTPUT
          echo "error_message=Failed to deploy to Pantheon. Check the deployment logs for details." >> $GITHUB_OUTPUT
        elif [[ "${{ steps.last_backup.conclusion }}" == "failure" ]]; then
          echo "failed_step=Backup Check" >> $GITHUB_OUTPUT
          echo "error_message=Failed to check or create backup on live environment." >> $GITHUB_OUTPUT
        elif [[ "${{ steps.create_backup.conclusion }}" == "failure" ]]; then
          echo "failed_step=Backup Creation" >> $GITHUB_OUTPUT
          echo "error_message=Failed to create backup on live environment." >> $GITHUB_OUTPUT
        elif [[ "${{ steps.verify_backup.conclusion }}" == "failure" ]]; then
          echo "failed_step=Backup Verification" >> $GITHUB_OUTPUT
          echo "error_message=No new LIVE backup appeared within ${{ inputs.backup_wait_minutes }} minutes. Check the Pantheon workflow log for the backup and re-run once it exists." >> $GITHUB_OUTPUT
        elif [[ "${{ steps.drush_commands.conclusion }}" == "failure" ]]; then
          echo "failed_step=Drush Commands" >> $GITHUB_OUTPUT
          echo "error_message=Failed to run database updates, config import, or cache rebuild." >> $GITHUB_OUTPUT
        elif [[ "${{ steps.env_url.conclusion }}" == "failure" ]]; then
          echo "failed_step=Environment URL" >> $GITHUB_OUTPUT
          echo "error_message=Failed to retrieve environment URL from Pantheon." >> $GITHUB_OUTPUT
        else
          echo "failed_step=Unknown" >> $GITHUB_OUTPUT
          echo "error_message=Deployment failed at an unknown step. Check the workflow logs." >> $GITHUB_OUTPUT
        fi

  notify_slack:
    name: Notify Slack
    needs: [semantic_release, deploy_to_pantheon]
    if: ${{ always() && inputs.slack_channel }}
    runs-on: ubuntu-latest
    steps:
    - name: Determine deployment status
      id: status
      run: |
        semantic_result="${{ needs.semantic_release.result }}"
        deploy_result="${{ needs.deploy_to_pantheon.result }}"

        if [[ "$deploy_result" == "success" ]]; then
          echo "status=success" >> $GITHUB_OUTPUT
          echo "color=good" >> $GITHUB_OUTPUT
          echo "emoji=:white_check_mark:" >> $GITHUB_OUTPUT
        elif [[ "$deploy_result" == "failure" ]]; then
          echo "status=failure" >> $GITHUB_OUTPUT
          echo "color=danger" >> $GITHUB_OUTPUT
          echo "emoji=:x:" >> $GITHUB_OUTPUT
        elif [[ "$deploy_result" == "cancelled" ]]; then
          echo "status=cancelled" >> $GITHUB_OUTPUT
          echo "color=warning" >> $GITHUB_OUTPUT
          echo "emoji=:warning:" >> $GITHUB_OUTPUT
        else
          echo "status=unknown" >> $GITHUB_OUTPUT
          echo "color=#808080" >> $GITHUB_OUTPUT
          echo "emoji=:question:" >> $GITHUB_OUTPUT
        fi

    - name: Send Slack notification
      if: ${{ always() }}
      run: |
        # Determine status and message
        # Check semantic release first, as deploy may be skipped if it fails
        if [[ "${{ needs.semantic_release.result }}" == "failure" ]]; then
          STATUS_EMOJI=":x:"
          STATUS_COLOR="danger"
          STATUS_TEXT="Failed"
          PHASE="Semantic Release"
        elif [[ "${{ needs.deploy_to_pantheon.result }}" == "success" ]]; then
          STATUS_EMOJI=":white_check_mark:"
          STATUS_COLOR="good"
          STATUS_TEXT="Success"
          PHASE="Deployment"
        elif [[ "${{ needs.deploy_to_pantheon.result }}" == "failure" ]]; then
          STATUS_EMOJI=":x:"
          STATUS_COLOR="danger"
          STATUS_TEXT="Failed"
          PHASE="Deployment"
        elif [[ "${{ needs.deploy_to_pantheon.result }}" == "cancelled" ]]; then
          STATUS_EMOJI=":warning:"
          STATUS_COLOR="warning"
          STATUS_TEXT="Cancelled"
          PHASE="Deployment"
        elif [[ "${{ needs.semantic_release.result }}" == "cancelled" ]]; then
          STATUS_EMOJI=":warning:"
          STATUS_COLOR="warning"
          STATUS_TEXT="Cancelled"
          PHASE="Semantic Release"
        else
          STATUS_EMOJI=":question:"
          STATUS_COLOR="#808080"
          STATUS_TEXT="Unknown"
          PHASE="Unknown"
        fi

        # Build the message
        ENV_URL_FIELD=""
        if [[ "${{ needs.deploy_to_pantheon.result }}" == "success" && -n "${{ needs.deploy_to_pantheon.outputs.env_url }}" ]]; then
          ENV_URL_FIELD=",{\"title\": \"Environment URL\", \"value\": \"<${{ needs.deploy_to_pantheon.outputs.env_url }}|${{ needs.deploy_to_pantheon.outputs.env_url }}>\", \"short\": false}"
        fi

        # Backup duration on success (S360-1076) — slow sites become visible.
        BACKUP_FIELD=""
        BACKUP_SECONDS="${{ needs.deploy_to_pantheon.outputs.backup_seconds }}"
        if [[ "${{ needs.deploy_to_pantheon.result }}" == "success" && -n "$BACKUP_SECONDS" ]]; then
          BACKUP_FIELD=",{\"title\": \"LIVE backup\", \"value\": \"took $((BACKUP_SECONDS/60))m$((BACKUP_SECONDS%60))s\", \"short\": true}"
        fi

        # Add error information if deployment failed
        ERROR_FIELD=""
        if [[ "${{ needs.deploy_to_pantheon.result }}" == "failure" ]]; then
          FAILED_STEP="${{ needs.deploy_to_pantheon.outputs.failed_step }}"
          ERROR_MSG="${{ needs.deploy_to_pantheon.outputs.error_message }}"
          if [[ -n "$FAILED_STEP" && -n "$ERROR_MSG" ]]; then
            # Properly escape JSON values using jq
            FAILED_STEP_ESCAPED=$(echo "$FAILED_STEP" | jq -Rs .)
            ERROR_MSG_ESCAPED=$(echo "$ERROR_MSG" | jq -Rs .)
            ERROR_FIELD=",{\"title\": \"Failed Step\", \"value\": ${FAILED_STEP_ESCAPED}, \"short\": true},{\"title\": \"Error Details\", \"value\": ${ERROR_MSG_ESCAPED}, \"short\": false}"
          else
            ERROR_FIELD=",{\"title\": \"Error Details\", \"value\": \"Deployment failed. Check the workflow logs for more details.\", \"short\": false}"
          fi
        elif [[ "${{ needs.semantic_release.result }}" == "failure" ]]; then
          ERROR_FIELD=",{\"title\": \"Error Details\", \"value\": \"Semantic release failed. Deployment was not attempted. Check the workflow logs for more details.\", \"short\": false}"
        fi

        # Send to Slack using Bot API
        if [[ -n "${{ secrets.SLACK_BOT_TOKEN }}" ]]; then
          curl -X POST -H "Authorization: Bearer ${{ secrets.SLACK_BOT_TOKEN }}" \
            -H 'Content-type: application/json; charset=utf-8' \
            --data "{
              \"channel\": \"${{ inputs.slack_channel }}\",
              \"text\": \"${STATUS_EMOJI} Deployment ${STATUS_TEXT}\",
              \"attachments\": [
                {
                  \"color\": \"${STATUS_COLOR}\",
                  \"fields\": [
                    {
                      \"title\": \"Repository\",
                      \"value\": \"${{ github.repository }}\",
                      \"short\": true
                    },
                    {
                      \"title\": \"Environment\",
                      \"value\": \"${{ inputs.target_env }}\",
                      \"short\": true
                    },
                    {
                      \"title\": \"Site\",
                      \"value\": \"${{ inputs.pantheon_site }}\",
                      \"short\": true
                    },
                    {
                      \"title\": \"Status\",
                      \"value\": \"${STATUS_TEXT}\",
                      \"short\": true
                    },
                    {
                      \"title\": \"Phase\",
                      \"value\": \"${PHASE}\",
                      \"short\": true
                    },
                    {
                      \"title\": \"Workflow\",
                      \"value\": \"<https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}|View Workflow>\",
                      \"short\": false
                    }
                    ${ENV_URL_FIELD}${BACKUP_FIELD}
                    ${ERROR_FIELD}
                  ],
                  \"footer\": \"GitHub Actions\",
                  \"ts\": $(date +%s)
                }
              ]
            }" \
            https://slack.com/api/chat.postMessage
        else
          echo "SLACK_BOT_TOKEN not provided, skipping Slack notification"
        fi
